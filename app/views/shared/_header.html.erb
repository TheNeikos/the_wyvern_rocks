<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">The Wyvern Rocks</a>

      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "Members", all_members_path %>
        </li>
        <li>
          <%= link_to "Blogs", blogs_path, class: "dropdown-toggle", 'data-toggle' => "dropdown" %>
          <ul class="dropdown-menu">
            <li>
              <%= link_to "Index", blogs_path %>
            </li>
            <% if policy(Blog).create? %>
              <li>
                <%= link_to "New", new_blog_path %>
              </li>
            <% end %>
          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% unless signed_in? %>
          <%= render "shared/header_login" %>
        <% else %>
          <li>
            <a class="dropdown-toggle" href="#" data-toggle="dropdown" role="button">
              <%= if current_member.name.present? then current_member.name.titleize end %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "View profile", member_page_path(current_member) %></li>
              <li><%= link_to "Edit profile", edit_member_registration_path %></li>
                <li class="divider"></li>
              <li><%= link_to 'Sign Out', destroy_member_session_path, method: :delete %></li>
            </ul>
          </li>
    </div>
  <% end %>
      </ul>
  </div>
  </div>
</nav>
