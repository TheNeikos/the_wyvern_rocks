CKEDITOR.editorConfig = function(config) {

    config.toolbar = [
        ["Source", "-", "Bold", "Italic", "Underline", "Strike", "-", "JustifyCenter"]
    ];

    config.extraPlugins = "bbcode,divarea";
}
